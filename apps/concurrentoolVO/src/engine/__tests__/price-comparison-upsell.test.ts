/**
 * Phase 27 Wave 0 scaffold — R10 calculateBasisPlusUpsell.
 *
 * Skeleton placeholders for the upsell engine that computes the
 * Basis vs Plus delta and conditionally surfaces the upsell based on
 * customerType. Implementation lands in Plan 27-09.
 */
import { describe, it } from 'vitest';

describe('calculateBasisPlusUpsell (R10)', () => {
  it.todo('computes the per-student delta between Basis and Plus packages');

  it.todo('surfaces the upsell only when customerType="bestaande-klant"');

  it.todo('hides the upsell when customerType="nieuwe-klant"');

  it.todo('is a pure function — same inputs always produce the same output');

  it.todo('returns a stable result when called repeatedly with shared inputs');

  it.todo('returns null when the school has no Plus-eligible modules selected');
});
