/**
 * Phase 27 Wave 0 scaffold — R2 Stichting PDF aggregation.
 *
 * Skeleton placeholders for the per-stichting PDF export that aggregates
 * 3+ schools into one DMU-targeted report. Implementation lands in
 * Plan 27-04.
 */
import { describe, it } from 'vitest';

describe('Stichting PDF aggregation (R2)', () => {
  it.todo('renders sections for each of the linked schools');

  it.todo('computes aggregated totals (financialDifference, totalAnnualValue) across schools');

  it.todo('allows the consultant to pick a DMU-role per export run');

  it.todo('renders the stichting cover page with naam + linked-school count');

  it.todo('skips schools without a completed wizard and surfaces them in a footer note');
});
