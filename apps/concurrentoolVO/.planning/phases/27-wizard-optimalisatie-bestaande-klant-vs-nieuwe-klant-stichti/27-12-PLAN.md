---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 12
type: execute
wave: 8
depends_on: ["27-01", "27-02", "27-03", "27-04", "27-05", "27-06", "27-07", "27-08", "27-09", "27-10", "27-11", "27-13", "27-14", "27-15", "27-16"]
files_modified:
  - e2e/stichting-crud.spec.ts
  - e2e/wizard-phase-27.spec.ts
  - apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-UAT.md
autonomous: false
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9, R10, R11]
tags: [e2e, uat, verification, cito-oud-grep-gate, final-gate]

must_haves:
  truths:
    - "E2E test stichting-crud passes (Playwright): create stichting, edit name, link 3 schools via bulk dialog, unlink 1, delete fails when schools linked, unlink all then delete works"
    - "E2E test wizard-phase-27 passes (Playwright): full wizard happy path Stap 1-5 met klant-type + schoolsoort + groei + huidig-gebruik + opmerkingen + pijnpunt-match + scenario Basis+Plus+Upsell"
    - "Cito-oud grep gate: grep -ri cito-oud apps/concurrentoolVO/src/ levert 0 hits buiten geaccepteerde test-fixtures"
    - "Migration engines grep gate: ls apps/concurrentoolVO/src/engine/migration.ts levert geen file"
    - "npm run build slaagt"
    - "npx vitest run slaagt: alle tests groen + coverage thresholds gehandhaafd (baseline 27/18/25/27)"
    - "User-UAT-walkthrough afgevinkt: stichting aanmaken naar 3 scholen naar wizard per school naar pijnpunt-match naar Upsell naar bulk-export werkt manueel"
  artifacts:
    - path: "e2e/stichting-crud.spec.ts"
      provides: "Volledige E2E test (vervangt Plan 01 scaffold)"
      min_lines: 80
    - path: "e2e/wizard-phase-27.spec.ts"
      provides: "Wizard happy-path E2E (vervangt Plan 01 scaffold)"
      min_lines: 100
    - path: "apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-UAT.md"
      provides: "UAT checklist met handmatige user-verificatie stappen"
      min_lines: 40
  key_links:
    - from: "e2e/wizard-phase-27.spec.ts"
      to: "src/features/school-profile/components/PainPointPanel.tsx"
      via: "clicks 'Vind Cito-voordelen' knop"
      pattern: "Vind Cito-voordelen"
    - from: "e2e/stichting-crud.spec.ts"
      to: "src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx"
      via: "clicks 'Scholen koppelen' + checks suggestions"
      pattern: "Scholen koppelen"
---

<objective>
Finale verificatie van Phase 27: end-to-end Playwright tests (vervangen Plan 01 scaffolds), greppen-gate voor cito-oud cleanup (R10 acceptance), build+test gate, en user-UAT-walkthrough document.

Purpose: SPEC acceptance criteria zegt expliciet: "npx playwright test slaagt voor nieuwe stichting-e2e + bestaande wizard-e2e" en "Handmatige UAT-walkthrough afgevinkt door gebruiker". Deze plan sluit de phase af.

Output: 2 echte Playwright spec files (geen scaffolds meer) + 1 UAT-checklist document + finale grep-gate verificatie + user-checkpoint voor handmatige walkthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-CONTEXT.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/playwright.config.ts
@apps/concurrentoolVO/e2e/

<interfaces>
<!-- Plan 01 leverde scaffolds in e2e/stichting-crud.spec.ts en e2e/wizard-phase-27.spec.ts met test.skip() — deze plan vervangt met echte tests -->

Test environment opvoer:
- `npx playwright test` runt tests in playwright.config.ts (verify devServer/baseURL config)
- Tests gebruiken Supabase test-instance OF mock — verify bestaand pattern (Phase 22 setup)
- Per Phase 22: TanStack Router test wrapper pattern (createMemoryHistory niet meer hard-coded paths /migratie of /huidig-vs-cito — Plan 10 verwijderde)

R-cross-coverage in deze plan (alle 11 R's):
- R1: stichting-crud.spec.ts dekt CRUD
- R2: bulk-export tab manual UAT
- R3, R4: wizard-phase-27.spec.ts klant-type + schoolsoort + groei velden
- R5: wizard-phase-27.spec.ts currentToolUsage per niveau
- R6: wizard-phase-27.spec.ts checks burgerschap + digi-gel toggle in Stap 3
- R7: wizard-phase-27.spec.ts checks 2 secties zichtbaar in Stap 3
- R8: wizard-phase-27.spec.ts checks summary-block + opmerkingen + time-input
- R9: wizard-phase-27.spec.ts clicks "Vind Cito-voordelen" + check matches verschijnen
- R10: wizard-phase-27.spec.ts kiest Basis/Plus/Upsell scenario in Stap 5 + grep-gate cito-oud
- R11: stichting-crud.spec.ts bulk-link 3 schools
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: E2E test — Stichting CRUD + bulk-link + cascade-guard (vervangt Plan 01 scaffold)</name>
  <read_first>
    - apps/concurrentoolVO/e2e/stichting-crud.spec.ts (Plan 01 scaffold met test.skip)
    - apps/concurrentoolVO/playwright.config.ts (devServer + baseURL + auth-setup)
    - apps/concurrentoolVO/e2e/ (lijst bestaande spec files voor pattern-spiegeling, bv. wizard-flow.spec.ts uit Phase 22)
    - apps/concurrentoolVO/e2e/fixtures/ (bestaande test-fixtures voor login + seed-data — Phase 22 patroon)
  </read_first>
  <files>e2e/stichting-crud.spec.ts</files>
  <action>
    Vervang `test.skip()` met echte Playwright test-suite:

    ```
    import { test, expect } from '@playwright/test';

    test.describe('Stichting CRUD flow (R1 + R11)', () => {
      test.beforeEach(async ({ page }) => {
        // Login + seed 5 demo schools via bestaand fixture-pattern
      });

      test('user creates a stichting and sees it in card-grid (R1)', async ({ page }) => {
        await page.goto('/stichtingen');
        await page.getByRole('button', { name: /Nieuwe stichting/i }).click();
        await page.getByLabel(/Naam stichting/i).fill('Stichting OBOS');
        await page.getByLabel(/Regio/i).fill('Utrecht');
        await page.getByRole('button', { name: /Aanmaken/i }).click();
        await expect(page.getByText('Stichting OBOS')).toBeVisible();
      });

      test('user edits stichting name (R1)', async ({ page }) => { /* navigate to detail, click Bewerken, change name, save */ });

      test('user links 3 schools via bulk dialog (R11)', async ({ page }) => {
        // Navigate to stichting detail
        // Click "Scholen koppelen"
        // Verify suggestion-list renders (smart-suggestion)
        // Manual-select 3 schools
        // Click Koppelen
        // Verify 3 schools in Scholen-tab
      });

      test('user unlinks 1 school via per-row action (R11 reverse)', async ({ page }) => { /* */ });

      test('delete fails when schools are still linked (D-04 cascade-guard)', async ({ page }) => {
        // Try delete stichting with 2 linked schools
        // Verify dialog: "Eerst N scholen loskoppelen..."
        // Verify stichting still exists
      });

      test('delete succeeds after unlinking all schools', async ({ page }) => { /* */ });
    });
    ```

    Gebruik bestaande auth-fixture en seed-data fixture uit Phase 22 (test environment). Als geen fixtures bestaan: maak inline seed via Supabase test-client.

    Test-timeouts: standaard Playwright 30s. AI-call testen niet hier (uses real network) — wel mock AI in unit-tests (Plan 09).
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npx playwright test e2e/stichting-crud.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 6 e2e tests groen
    - Alle CRUD + bulk-link + cascade-guard scenarios gedekt
    - Geen flaky tests (3 retries clean)
  </acceptance_criteria>
  <done>R1 + R11 e2e gedekt. SPEC acceptance "Playwright e2e voor stichting-CRUD flow" voldaan.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: E2E test — Wizard happy-path R3 t/m R10 (vervangt Plan 01 scaffold) + cito-oud grep gate</name>
  <read_first>
    - apps/concurrentoolVO/e2e/wizard-phase-27.spec.ts (Plan 01 scaffold met test.skip)
    - Bestaande wizard e2e tests (Phase 22) voor patroon-spiegeling
  </read_first>
  <files>e2e/wizard-phase-27.spec.ts</files>
  <action>
    Vervang `test.skip()` met echte wizard happy-path:

    ```
    import { test, expect } from '@playwright/test';

    test.describe('Wizard Phase 27 happy path (R3-R10)', () => {
      test.beforeEach(async ({ page }) => {
        // Login + create fresh school
      });

      test('full wizard flow met alle Phase 27 features', async ({ page }) => {
        // Stap 1 — klant-type + schoolsoort + groei (R3, R4)
        await page.getByLabel(/Klant-type/i).getByText(/Huidige Cito-klant/i).click();
        await page.getByLabel(/Schoolsoort/i).selectOption('dakpanklas');
        await page.getByLabel(/Groei/i).getByText(/Groeit/i).click();
        await page.getByRole('button', { name: /Volgende/i }).click();

        // Stap 2 — student counts + huidig-gebruik per niveau (R5)
        await page.locator('[data-level="VMBO-B"]').getByRole('radio', { name: /Cito/i }).check();
        // ... fill matrix + Volgende

        // Stap 3 — modules in 2 secties (R6, R7)
        await expect(page.getByRole('heading', { name: /Basisvaardigheden/i })).toBeVisible();
        await expect(page.getByRole('heading', { name: /Extra Modules/i })).toBeVisible();
        await page.getByLabel(/Burgerschap/i).check();
        await page.getByLabel(/Digitale geletterdheid/i).check();
        await page.getByRole('button', { name: /Volgende/i }).click();

        // Stap 4 — summary + opmerkingen + tijd + AI matching (R8, R9)
        await expect(page.getByText(/Schoolgegevens/i)).toBeVisible();
        await page.getByPlaceholder(/pijnpunten/i).fill('Rapportages zijn onduidelijk en support reageert traag');
        await page.getByRole('button', { name: /Vind Cito-voordelen/i }).click();
        // Rule-based should appear instant (< 100ms)
        await expect(page.getByText(/rapportage-helder|nederlandse-helpdesk/i).first()).toBeVisible({ timeout: 200 });

        // Fill TimeInputSection competitor-time mode
        await page.locator('[data-task="rechten"]').fill('30');
        await page.getByRole('button', { name: /Volgende/i }).click();

        // Stap 5 — Basis/Plus/Upsell scenario (R10)
        await expect(page.getByText(/Basis vs concurrent/i)).toBeVisible();
        await expect(page.getByText(/Plus vs concurrent/i)).toBeVisible();
        await expect(page.getByText(/Upsell/i)).toBeVisible();  // visible because customerType=huidige-cito
        await page.getByText(/Upsell/i).click();
        await expect(page.getByText(/delta|per jaar/i)).toBeVisible();

        await page.getByRole('button', { name: /Afronden/i }).click();

        // Verify all data persisted: navigate back to school detail
        // ...
      });

      test('Upsell scenario NIET zichtbaar voor nieuwe-prospect (R10 conditional)', async ({ page }) => {
        // Fresh school, customerType = nieuwe-prospect
        // Walk to Stap 5
        // Expect Upsell card NOT visible
        await expect(page.getByText(/Upsell/i)).not.toBeVisible();
      });
    });
    ```

    **Geen** route-tests voor `/migratie` of `/huidig-vs-cito` — Plan 10 verwijderde die. Als bestaande e2e specs deze routes nog gebruiken: update naar verwijderen.
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npx playwright test e2e/wizard-phase-27.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Wizard happy-path e2e groen (covers R3-R10 in single flow)
    - Conditional Upsell-test groen (R10 customerType conditional)
    - Geen flaky tests
  </acceptance_criteria>
  <done>R3-R10 e2e gedekt. SPEC acceptance "wizard-flow e2e" voldaan.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final gate — grep-gate + build/test gate + UAT-walkthrough</name>
  <read_first>
    - apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md (acceptance criteria onderkant)
  </read_first>
  <what-built>
    Executor heeft afgerond:
    1. **Cito-oud grep gate** — run `grep -rni 'cito-oud' apps/concurrentoolVO/src/ | grep -v 'Binary' | head -20`
       - Expected: 0 hits (R10 acceptance)
       - Als hits gevonden: lijst presenteren, beslissen per hit of legitiem fixture-comment of bug
    2. **Migration engines grep gate** — run `ls apps/concurrentoolVO/src/engine/migration.ts apps/concurrentoolVO/src/engine/current-vs-proposed.ts apps/concurrentoolVO/src/data/cito-migration-prices.ts 2>&1`
       - Expected: 3x "No such file or directory"
    3. **Build gate** — `cd apps/concurrentoolVO && npm run build` → expected: groen
    4. **Test gate** — `cd apps/concurrentoolVO && npx vitest run` → expected: groen, coverage thresholds gehandhaafd (baseline 27/18/25/27 per Phase 22)
    5. **E2E gate** — `cd apps/concurrentoolVO && npx playwright test` → expected: alle specs groen (stichting-crud + wizard-phase-27 + bestaande Phase 22 specs)
    6. **UAT-document** — Maak `.planning/phases/27-.../27-UAT.md` met de SPEC acceptance criteria als checkboxes voor user-walkthrough:

       ```
       # Phase 27 — UAT Walkthrough Checklist

       Open de app op productie-URL (of `npm run dev` lokaal). Voltooi onderstaande stappen en vink af.

       ## Stichting feature (R1, R2, R11)
       - [ ] /stichtingen route opent en toont card-grid
       - [ ] "Nieuwe stichting" knop opent dialog
       - [ ] Stichting aanmaken werkt (Stichting OBOS, regio Utrecht)
       - [ ] Detail-view toont 3 tabs (Overzicht/Scholen/Export)
       - [ ] "Scholen koppelen" suggestions verschijnen
       - [ ] Bulk-koppel 3 scholen werkt
       - [ ] Loskoppel 1 school werkt
       - [ ] Delete stichting met scholen toont waarschuwing
       - [ ] Delete stichting zonder scholen werkt
       - [ ] CSV-export downloadt valide bestand
       - [ ] DMU-PDF-export per rol genereert PDF met aggregatie

       ## Wizard sales-context (R3, R4, R5)
       - [ ] Stap 1: klant-type vinkje aanwezig met 3 opties
       - [ ] Stap 1: schoolsoort dropdown met 6 opties + customSchoolType bij 'overig'
       - [ ] Stap 1: groei-trajectorie radio met 4 opties
       - [ ] Stap 2: huidig-gebruik radio per niveau verschijnt na level-selectie

       ## Module-catalogus (R6, R7)
       - [ ] Stap 3: 2 expliciete secties "Basisvaardigheden" + "Extra Modules"
       - [ ] Burgerschap module zichtbaar in Extra Modules
       - [ ] Digitale geletterdheid module zichtbaar in Extra Modules

       ## Stap 4 features (R8, R9)
       - [ ] Summary-blok bovenaan met 3 sub-blokken
       - [ ] Wijzig-knop in school-blok navigeert naar Stap 1
       - [ ] Opmerkingen-textarea met 1000-char-counter
       - [ ] "Vind Cito-voordelen" knop werkt
       - [ ] Rule-based matches binnen 100ms
       - [ ] AI matches verschijnen binnen 5s (of badge "AI niet beschikbaar")
       - [ ] Thumb-up/down feedback persisteert
       - [ ] TimeInputSection competitor-time mode zichtbaar

       ## Stap 5 scenarios (R10)
       - [ ] customerType=huidige-cito toont 3 scenarios
       - [ ] customerType=nieuwe-prospect toont 2 scenarios (geen Upsell)
       - [ ] Upsell preview toont delta-per-jaar + features
       - [ ] Geen "migratie" of "huidig vs Cito" knoppen meer

       ## Grep gates (R10)
       - [ ] `grep -ri cito-oud apps/concurrentoolVO/src/` = 0 hits
       - [ ] `ls apps/concurrentoolVO/src/engine/migration.ts` faalt (file weg)
       ```

    Executor presenteert alle gate-results + UAT-document path.
  </what-built>
  <how-to-verify>
    User loopt UAT-checklist door in de browser:
    1. Lokaal: `cd apps/concurrentoolVO && npm run dev` of preview-URL
    2. Vink elk item af in 27-UAT.md
    3. Als items falen: maak notities + escalate naar gap-closure plan (run `/gsd-plan-phase 27 --gaps`)
    4. Als alle items groen: type "UAT approved — Phase 27 done"
  </how-to-verify>
  <resume-signal>Type "UAT approved — Phase 27 done" OF lijst van gefaalde items voor gap-closure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → app | Authed headless browser session |
| Manual UAT → app | User browser session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12-01 | Tampering | E2E test seeds productie-Supabase per ongeluk | mitigate | playwright.config.ts gebruikt test-instance URL (Phase 22 baseline). Verify in execute. |
| T-27-12-02 | DoS | E2E test floods AI endpoint | mitigate | wizard-phase-27 spec mockt AI of accepteert rule-based-only path. Geen real anthropic-call in CI. |
| T-27-12-03 | Information Disclosure | UAT-document checks production data | accept | UAT-walkthrough op test/staging eerst, dan productie. User trained. |
</threat_model>

<verification>
- `npx playwright test` groen — alle specs
- `npm run build` groen
- `npx vitest run` groen + coverage thresholds gehandhaafd
- Cito-oud grep gate: 0 non-test hits
- UAT-walkthrough document gemaakt
- User UAT approved
</verification>

<success_criteria>
- Alle 11 R's gedekt door e2e + UAT
- SPEC acceptance criteria 14 items afgevinkt
- Phase 27 STATE.md kan bijgewerkt naar "Complete"
- Volgende phase (28: Win/loss-tracking) kan ontgrendeld worden
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-12-SUMMARY.md` + finalize 27-UAT.md afgevinkt door user.
</output>
