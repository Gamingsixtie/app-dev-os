---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - src/models/school.ts
  - src/db/types.ts
  - src/db/database.ts
  - supabase/migrations/015_school_sales_context.sql
  - src/features/school-profile/schemas/school-meta.schema.ts
  - src/features/school-profile/schemas/step1-schema.ts
  - src/features/school-profile/components/CustomerTypeRadio.tsx
  - src/features/school-profile/components/SchoolTypeFields.tsx
  - src/features/school-profile/components/GrowthTrajectoryRadio.tsx
  - src/features/school-profile/components/WizardStep1.tsx
  - src/features/school-profile/store.ts
  - src/features/school-profile/components/__tests__/WizardStep1.test.tsx
autonomous: true
requirements: [R3, R4]
tags: [wizard-step1, customer-type, school-type, growth-trajectory]

must_haves:
  truths:
    - "WizardStep1 toont radio 'Klant-type' met 3 opties (huidige-cito / nieuwe-prospect / gedeeltelijk)"
    - "WizardStep1 toont select 'Schoolsoort' met 6 opties (regulier/dakpanklas/dalton/montessori/vrije-school/overig)"
    - "Bij schoolType=overig verschijnt veld customSchoolType"
    - "WizardStep1 toont radio 'Groei-trajectorie' met 4 opties"
    - "Alle 4 nieuwe velden persisteren in Dexie + Supabase via store"
    - "Zod-validatie blokkeert Next bij missende verplichte velden"
  artifacts:
    - path: "src/features/school-profile/schemas/school-meta.schema.ts"
      provides: "schoolMetaSchema met customerType + schoolType + growthTrajectory + customSchoolType refine"
      min_lines: 20
    - path: "src/features/school-profile/components/CustomerTypeRadio.tsx"
      provides: "Radio component R3"
      min_lines: 25
    - path: "src/features/school-profile/components/SchoolTypeFields.tsx"
      provides: "Select + conditional customSchoolType input R4"
      min_lines: 25
    - path: "src/features/school-profile/components/GrowthTrajectoryRadio.tsx"
      provides: "Radio component R4"
      min_lines: 25
    - path: "supabase/migrations/015_school_sales_context.sql"
      provides: "ALTER schools customer_type + school_type + custom_school_type + growth_trajectory"
      contains: "ALTER TABLE schools"
  key_links:
    - from: "src/features/school-profile/components/WizardStep1.tsx"
      to: "src/features/school-profile/components/CustomerTypeRadio.tsx"
      via: "JSX render"
      pattern: "CustomerTypeRadio"
    - from: "src/features/school-profile/components/WizardStep1.tsx"
      to: "src/features/school-profile/store.ts"
      via: "store setters"
      pattern: "setCustomerType|setSchoolType|setGrowthTrajectory"
---

<objective>
Implementeer R3 (klant-type vinkje) en R4 (schoolsoort + groei-trajectorie) in WizardStep1. Drie sub-components (D-17), gedeelde Zod schema school-meta.schema.ts (D-18 .merge()), persistentie via SchoolRecord uitbreiding + Supabase migratie 015 + Dexie v3 upgrade-uitbreiding.

Purpose: customerType drijft Plan 10 (R10) Upsell-scenario visibility. schoolType + growthTrajectory zijn sales-context voor stichting-rapportages.

Output: WizardStep1 krijgt 3 nieuwe veld-secties. Zod schema validatie. Store persisteert. Test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-CONTEXT.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/src/features/school-profile/components/WizardStep1.tsx
@apps/concurrentoolVO/src/features/school-profile/schemas/step1-schema.ts
@apps/concurrentoolVO/src/features/school-profile/store.ts
@apps/concurrentoolVO/src/models/school.ts

<interfaces>
Zod schema (D-18):
```
export const customerTypeSchema = z.enum(['huidige-cito', 'nieuwe-prospect', 'gedeeltelijk']);
export const schoolTypeEnum = z.enum(['regulier', 'dakpanklas', 'dalton', 'montessori', 'vrije-school', 'overig']);
export const growthTrajectorySchema = z.enum(['groei', 'krimp', 'stabiel', 'loting']);

export const schoolMetaSchema = z.object({
  customerType: customerTypeSchema,
  schoolType: schoolTypeEnum,
  customSchoolType: z.string().max(50).optional(),
  growthTrajectory: growthTrajectorySchema,
}).refine(
  (d) => d.schoolType !== 'overig' || (d.customSchoolType && d.customSchoolType.length > 0),
  { message: 'Vul de naam van het schooltype in', path: ['customSchoolType'] },
);

// step1-schema EXTEND:
export const schoolTypeSchema = z.object({
  schoolName: z.string().min(2).max(100),
  levels: z.array(z.enum(SCHOOL_LEVELS)).min(1),
}).merge(schoolMetaSchema);
```

Dutch labels (locked):
- CUSTOMER_TYPE_LABELS: { 'huidige-cito': 'Huidige Cito-klant', 'nieuwe-prospect': 'Nieuwe prospect', 'gedeeltelijk': 'Gedeeltelijk' }
- SCHOOL_TYPE_LABELS: { regulier: 'Regulier', dakpanklas: 'Dakpanklas', dalton: 'Daltonschool', montessori: 'Montessorischool', 'vrije-school': 'Vrije school', overig: 'Anders, namelijk:' }
- GROWTH_TRAJECTORY_LABELS: { groei: 'Groeit', krimp: 'Krimpt', stabiel: 'Stabiel', loting: 'Loting (mogelijke daling)' }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Data-laag — SchoolRecord uitbreiding + Supabase migratie 015 + Dexie v3 + store actions</name>
  <read_first>
    - apps/concurrentoolVO/src/models/school.ts (current SchoolRecord shape)
    - apps/concurrentoolVO/src/db/types.ts (current DexieSchoolRecord)
    - apps/concurrentoolVO/src/db/database.ts (post-Plan 02 — version(3) bestaat al)
    - apps/concurrentoolVO/src/features/school-profile/store.ts (current setters pattern)
    - apps/concurrentoolVO/supabase/migrations/ (Plan 02 schreef 014, dit plan schrijft 015)
  </read_first>
  <files>src/models/school.ts, src/db/types.ts, src/db/database.ts, supabase/migrations/015_school_sales_context.sql, src/features/school-profile/store.ts</files>
  <action>
    1. src/models/school.ts: voeg toe aan SchoolRecord customerType, schoolType, customSchoolType, growthTrajectory (allemaal optional + null). Plus 3 LABELS Records (Dutch).
    2. src/db/types.ts: DexieSchoolRecord krijgt zelfde 4 velden.
    3. src/db/database.ts: uitbreiden van bestaande version(3).upgrade()-callback (Plan 02 schreef basis) — append:
       ```
       school.customerType = school.customerType ?? null;
       school.schoolType = school.schoolType ?? null;
       school.customSchoolType = school.customSchoolType ?? null;
       school.growthTrajectory = school.growthTrajectory ?? null;
       ```
       NIET version(4). Idempotent.
    4. supabase/migrations/015_school_sales_context.sql: ALTER TABLE schools ADD COLUMN customer_type TEXT, school_type TEXT, custom_school_type TEXT, growth_trajectory TEXT. Allemaal nullable, geen check-constraint (Zod validates frontend).
    5. src/features/school-profile/store.ts: voeg toe setCustomerType, setSchoolType, setCustomSchoolType, setGrowthTrajectory. Pattern spiegelen op setSchoolName. Persist via bestaande updateSchoolData() flow.
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SchoolRecord heeft 4 nieuwe optional fields
    - Migratie 015 voegt 4 nullable kolommen toe
    - Dexie v3 upgrade idempotent (?? defaults)
    - Store heeft 4 nieuwe setters die persist triggeren
    - npm run build slaagt
  </acceptance_criteria>
  <done>Data-laag R3+R4 klaar.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UI — 3 sub-components + WizardStep1 integratie + Zod schema extension + tests</name>
  <read_first>
    - apps/concurrentoolVO/src/features/school-profile/components/WizardStep1.tsx (current shape)
    - apps/concurrentoolVO/src/features/school-profile/schemas/step1-schema.ts
    - apps/concurrentoolVO/src/features/school-profile/components/__tests__/WizardStep1.test.tsx (current tests)
    - apps/concurrentoolVO/src/components/ui/ (herbruikbare radio/select componenten)
  </read_first>
  <files>src/features/school-profile/schemas/school-meta.schema.ts, src/features/school-profile/schemas/step1-schema.ts, src/features/school-profile/components/CustomerTypeRadio.tsx, src/features/school-profile/components/SchoolTypeFields.tsx, src/features/school-profile/components/GrowthTrajectoryRadio.tsx, src/features/school-profile/components/WizardStep1.tsx, src/features/school-profile/components/__tests__/WizardStep1.test.tsx</files>
  <action>
    1. school-meta.schema.ts: kopieer Zod schema uit Interfaces-block.
    2. step1-schema.ts: .merge(schoolMetaSchema) op bestaand schoolTypeSchema.
    3. CustomerTypeRadio.tsx: stateless, props { value, onChange, error? }. 3 radio's met CUSTOMER_TYPE_LABELS. Label "Klant-type:" boven groep. ARIA-group. Dutch.
    4. SchoolTypeFields.tsx: stateless. Native select met 6 opties (SCHOOL_TYPE_LABELS). Bij schoolType==='overig' render extra input customSchoolType met placeholder. Label "Schoolsoort:".
    5. GrowthTrajectoryRadio.tsx: stateless. 4 opties (GROWTH_TRAJECTORY_LABELS). Label "Hoe ontwikkelt het leerlingaantal zich?".
    6. WizardStep1.tsx: audit per D-16. Behoud schoolName + levels. Voeg onder levels in volgorde: CustomerTypeRadio → SchoolTypeFields → GrowthTrajectoryRadio. Bij submit call store-setters (setCustomerType, setSchoolType, setCustomSchoolType alleen als 'overig' else null, setGrowthTrajectory). DefaultValues uit store (mirror schoolName/levels).
    7. __tests__/WizardStep1.test.tsx: vervang it.todo's met:
       - persists customerType to store
       - persists schoolType to store
       - shows customSchoolType only when schoolType=overig
       - blocks Next when growthTrajectory missing
       - blocks Next when schoolType=overig en customSchoolType empty
       Gebruik @testing-library/react + userEvent. Mock store via Phase 22 wrapper.
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10 && npx vitest run src/features/school-profile/components/__tests__/WizardStep1.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - WizardStep1 toont 3 nieuwe sub-component-secties
    - schoolType=overig triggert customSchoolType-input
    - Zod blokkeert submit bij missende velden
    - Store-setters worden aangeroepen op submit
    - 5 nieuwe tests groen + bestaande blijven groen
    - npm run build slaagt
    - Alle UI Dutch
  </acceptance_criteria>
  <done>R3 + R4 functioneel.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Browser → Supabase | Persistence van customer_type/school_type/etc velden |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | customSchoolType free-text injection naar PDF | mitigate | @react-pdf rendert plain string + Zod max(50) |
| T-27-03-02 | Information Disclosure | Sales-context cross-team leak | mitigate | RLS schools tabel beschermt nieuwe kolommen automatisch |
| T-27-03-03 | Input Validation | customerType buiten enum | mitigate | Zod enum + TEXT kolom (geen DB check-constraint per simplicity) |
</threat_model>

<verification>
- npm run build groen
- npx vitest run WizardStep1.test.tsx groen
- Bestaande tests blijven groen
- Manual smoke: wizard Stap 1 → kies alle nieuwe velden → Next → terug → waardes blijven (persistence)
</verification>

<success_criteria>
- R3: customerType vinkje aanwezig, 3 opties getest
- R4: schoolType + growthTrajectory aanwezig, customSchoolType conditional
- Plan 10 kan customerType !== 'nieuwe-prospect' checken voor Upsell-visibility
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-03-SUMMARY.md`
</output>
