---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 05
type: execute
wave: 3
depends_on: ["27-01", "27-03"]
files_modified:
  - src/models/school.ts
  - src/models/stichting.ts
  - src/db/types.ts
  - src/db/database.ts
  - supabase/migrations/016_current_tool_usage.sql
  - src/features/school-profile/schemas/step2-schema.ts
  - src/features/school-profile/components/CurrentToolPerLevel.tsx
  - src/features/school-profile/components/WizardStep2.tsx
  - src/features/school-profile/store.ts
  - src/features/school-profile/components/__tests__/WizardStep2.test.tsx
autonomous: true
requirements: [R5]
tags: [wizard-step2, current-tool-usage, per-level, stichting-aggregation]

must_haves:
  truths:
    - "WizardStep2 toont onder student-counts-matrix een sectie met radio-keuze per gekozen niveau"
    - "Elke niveau-rij heeft 5 opties: Cito / DIA / JIJ! / Mix / Geen"
    - "Aggregatie levert UsageMix per school op (cito-only / concurrent-only / mixed / unknown)"
    - "currentToolUsage persisteert in Dexie + Supabase per school"
    - "WizardStep2 validatie blokkeert Next niet (currentToolUsage is optioneel)"
  artifacts:
    - path: "src/features/school-profile/components/CurrentToolPerLevel.tsx"
      provides: "Stateless component met radio per niveau (D-17)"
      min_lines: 30
    - path: "supabase/migrations/016_current_tool_usage.sql"
      provides: "ALTER schools ADD COLUMN current_tool_usage JSONB"
      contains: "current_tool_usage"
  key_links:
    - from: "src/features/school-profile/components/WizardStep2.tsx"
      to: "src/features/school-profile/components/CurrentToolPerLevel.tsx"
      via: "JSX render onder student-counts-matrix"
      pattern: "CurrentToolPerLevel"
    - from: "src/features/stichtingen/StichtingDetailPage.tsx"
      to: "src/models/stichting.ts (getStichtingUsageMix)"
      via: "aggregatie over linked schools currentToolUsage"
      pattern: "getStichtingUsageMix"
---

<objective>
Implementeer R5: WizardStep2 capteert per gekozen niveau welk pakket de school nu gebruikt. Aggregatie op stichtingsniveau via `getStichtingUsageMix` helper (Plan 02 leverde placeholder, deze plan vult echte logica).

Purpose: Sales heeft per-niveau zicht op huidige situatie binnen één school. Stichting-aggregatie maakt cross-school mix zichtbaar op StichtingCard.

Output: Nieuw CurrentToolPerLevel component + WizardStep2 integratie + JSONB-veld op SchoolRecord + Supabase migratie 016 + Dexie v3 upgrade-uitbreiding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/src/features/school-profile/components/WizardStep2.tsx
@apps/concurrentoolVO/src/features/school-profile/schemas/step2-schema.ts
@apps/concurrentoolVO/src/models/school.ts
@apps/concurrentoolVO/src/models/stichting.ts

<interfaces>
```
type CurrentToolUsage = 'cito' | 'dia' | 'jij' | 'mix' | 'geen';
type CurrentToolUsageMap = Partial<Record<SchoolLevel, CurrentToolUsage>>;

const CURRENT_TOOL_USAGE_LABELS = {
  cito: 'Cito',
  dia: 'DIA',
  jij: 'JIJ!',
  mix: 'Mix (meerdere aanbieders)',
  geen: 'Geen (nieuwe markt)',
};
```

UsageMix aggregatie logic (getStichtingUsageMix):
- Collect all currentToolUsage values across schools × levels
- All === 'cito' → 'cito-only'
- All ∈ {'dia','jij'} → 'concurrent-only'
- Mix → 'mixed'
- Empty → 'unknown'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Data-laag — currentToolUsage JSONB + Supabase 016 + Dexie v3 + store action + Stichting aggregatie</name>
  <read_first>
    - apps/concurrentoolVO/src/models/school.ts (post-Plan 03 state)
    - apps/concurrentoolVO/src/db/database.ts (post-Plan 02/03 — version(3) bestaat)
    - apps/concurrentoolVO/src/features/school-profile/store.ts
    - apps/concurrentoolVO/src/models/stichting.ts (Plan 02 placeholder helper)
  </read_first>
  <files>src/models/school.ts, src/models/stichting.ts, src/db/types.ts, src/db/database.ts, supabase/migrations/016_current_tool_usage.sql, src/features/school-profile/store.ts</files>
  <action>
    1. src/models/school.ts: voeg toe currentToolUsage?: Partial<Record<SchoolLevel, 'cito'|'dia'|'jij'|'mix'|'geen'>>. Plus CURRENT_TOOL_USAGE_LABELS Record.
    2. src/db/types.ts: DexieSchoolRecord krijgt currentToolUsage.
    3. src/db/database.ts: uitbreiden version(3).upgrade()-callback: school.currentToolUsage = school.currentToolUsage ?? {}.
    4. supabase/migrations/016_current_tool_usage.sql: ALTER TABLE schools ADD COLUMN current_tool_usage JSONB NOT NULL DEFAULT '{}'::jsonb.
    5. src/features/school-profile/store.ts: setCurrentToolUsage(level, value) action (immer-style mutate map + persist). Optionele setCurrentToolUsageMap(map) bulk-set.
    6. src/models/stichting.ts: implementeer echte getStichtingUsageMix(schools): UsageMix logic uit Interfaces-block (Plan 02 had placeholder).
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SchoolRecord heeft currentToolUsage optional
    - Migratie 016 voegt JSONB kolom toe met default {}
    - Dexie v3 upgrade idempotent
    - Store heeft setCurrentToolUsage
    - getStichtingUsageMix returnt 4 mogelijke waardes correct
    - npm run build slaagt
  </acceptance_criteria>
  <done>Data-laag R5 + Stichting mix-aggregatie functioneel.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UI — CurrentToolPerLevel component + WizardStep2 integratie + Zod schema + tests</name>
  <read_first>
    - apps/concurrentoolVO/src/features/school-profile/components/WizardStep2.tsx (full file — student-counts matrix layout)
    - apps/concurrentoolVO/src/features/school-profile/schemas/step2-schema.ts
    - apps/concurrentoolVO/src/features/school-profile/components/__tests__/WizardStep2.test.tsx
  </read_first>
  <files>src/features/school-profile/components/CurrentToolPerLevel.tsx, src/features/school-profile/components/WizardStep2.tsx, src/features/school-profile/schemas/step2-schema.ts, src/features/school-profile/components/__tests__/WizardStep2.test.tsx</files>
  <action>
    1. CurrentToolPerLevel.tsx: stateless. Props { levels, value, onChange }. Render rij per level met label + 5 radios uit CURRENT_TOOL_USAGE_LABELS. ARIA-groups per niveau. Header: "Welk pakket gebruikt elk niveau nu?".
    2. step2-schema.ts: extend met `currentToolUsage: z.record(z.enum(SCHOOL_LEVELS), z.enum(['cito','dia','jij','mix','geen'])).default({})`. Geen .min() — optioneel.
    3. WizardStep2.tsx: audit per D-16. Behoud matrix + preset-buttons. Voeg toe onder matrix:
       ```
       <CurrentToolPerLevel
         levels={selectedLevels}
         value={watch('currentToolUsage') ?? {}}
         onChange={(level, val) => { setValue(`currentToolUsage.${level}`, val); setCurrentToolUsage(level, val); }}
       />
       ```
       selectedLevels uit store. Sectie alleen tonen als levels.length > 0.
    4. __tests__/WizardStep2.test.tsx: toevoegen:
       - renders one CurrentToolPerLevel row per selected level
       - persists currentToolUsage to store
       - does not show section when no levels selected
       - Next button enabled even when currentToolUsage empty
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10 && npx vitest run src/features/school-profile/components/__tests__/WizardStep2.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - WizardStep2 toont rij met 5 radios per geselecteerd niveau
    - Selectie persisteert via store
    - Niet-geselecteerde niveaus zijn afwezig
    - currentToolUsage optioneel (Next-knop blokkeert niet)
    - Tests groen
    - npm run build slaagt
    - UI Dutch
  </acceptance_criteria>
  <done>R5 compleet.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Browser → Supabase | JSONB currentToolUsage persistentie |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering | currentToolUsage onbestaande enum-waarde | mitigate | Zod enum frontend + Postgres tolerant JSONB. Worst case: aggregatie skipt unknown. |
| T-27-05-02 | Information Disclosure | Cross-team aggregatie | accept | Bestaande RLS schools-tabel beschermt |
</threat_model>

<verification>
- npm run build groen
- npx vitest run WizardStep2.test.tsx groen
- Manual smoke: Stap 1 selecteer 2 levels → Stap 2 → CurrentToolPerLevel toont 2 rijen → kies opties → Next → terug → waardes blijven
</verification>

<success_criteria>
- R5 acceptance: per niveau currentToolUsage keuze persisteert
- Stichting-aggregatie werkt: 3 scholen × 5 niveaus = 15 keuzes opgeteld geeft correcte mix-indicator
- StichtingCard kan nu `getStichtingUsageMix(linkedSchools)` aanroepen voor 3-dots-indicator
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-05-SUMMARY.md`
</output>
