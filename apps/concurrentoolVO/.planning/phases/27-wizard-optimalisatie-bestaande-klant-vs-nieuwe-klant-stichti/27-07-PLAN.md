---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 07
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - src/lib/stringSimilarity.ts
  - src/lib/stichtingMatcher.ts
  - src/lib/__tests__/stringSimilarity.test.ts
  - src/lib/__tests__/stichtingMatcher.test.ts
  - src/features/stichtingen/components/StichtingSuggestionList.tsx
  - src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx
  - src/features/stichtingen/StichtingDetailPage.tsx
  - src/db/operations.ts
  - src/features/stichtingen/hooks/useStichtingen.ts
autonomous: true
requirements: [R11]
tags: [bulk-link, smart-suggestion, levenshtein, no-npm-dep]

must_haves:
  truths:
    - "User kan op StichtingDetailPage 'Scholen koppelen' actie starten"
    - "Smart-suggestion toont voorgestelde matches met confidence score (regio + naam-similarity)"
    - "Suggesties met score > 0.8 zijn default checked, anders unchecked"
    - "User kan handmatig ongekoppelde scholen toevoegen via multi-select"
    - "Bulk-koppel zet stichting_id op N scholen in één Supabase transactie"
    - "Loskoppel-actie (per rij in Scholen-tab) zet stichting_id terug op null"
  artifacts:
    - path: "src/lib/stringSimilarity.ts"
      provides: "Inline Levenshtein similarity() (~50 LOC, geen npm dep)"
      min_lines: 30
    - path: "src/lib/stichtingMatcher.ts"
      provides: "suggestSchoolsForStichting() pure function"
      min_lines: 40
    - path: "src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx"
      provides: "Multi-select dialog met smart-suggestion + handmatig"
      min_lines: 60
  key_links:
    - from: "src/features/stichtingen/StichtingDetailPage.tsx"
      to: "src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx"
      via: "'Scholen koppelen' knop opent dialog"
      pattern: "BulkLinkSchoolsDialog"
    - from: "src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx"
      to: "src/lib/stichtingMatcher.ts"
      via: "suggestSchoolsForStichting()"
      pattern: "suggestSchoolsForStichting"
    - from: "src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx"
      to: "src/db/operations.ts"
      via: "bulkLinkSchools(stichtingId, schoolIds)"
      pattern: "bulkLinkSchools"
---

<objective>
Implementeer R11: bulk-migratie van bestaande schools naar Stichting via smart-suggestion (D-03) + handmatige multi-select. Inline Levenshtein (research-aanbeveling: geen npm dep). Plan 02 leverde data-laag basis; deze plan voegt bulkLinkSchools + smart-suggestion + UI dialog toe.

Purpose: 10 bestaande demo-scholen moeten in praktische bulk-flow gekoppeld kunnen worden. Smart-suggestion bespaart klikwerk bij naam/regio overlap.

Output: 2 nieuwe lib-files + BulkLinkSchoolsDialog + Stichting-detail integratie + bulkLinkSchools operation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-CONTEXT.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/src/features/stichtingen/StichtingDetailPage.tsx
@apps/concurrentoolVO/src/db/operations.ts
@apps/concurrentoolVO/src/models/school.ts

<interfaces>
Per RESEARCH § Standard Stack + § Code Example 2:

```
// src/lib/stringSimilarity.ts (Wagner-Fischer Levenshtein, public domain, ~50 LOC)
export function similarity(a, b): number;

// src/lib/stichtingMatcher.ts
export interface MatchSuggestion {
  schoolId: string;
  schoolName: string;
  score: number;       // 0-1
  preChecked: boolean; // score > 0.8
  reasons: string[];
}
export function suggestSchoolsForStichting(
  stichting: Pick<StichtingRecord, 'name'|'region'>,
  schools: SchoolRecord[],
): MatchSuggestion[];

// Weights (Claude's discretion):
const REGIO_WEIGHT = 0.35;
const NAAM_WEIGHT = 0.65;
const MIN_SCORE_SUGGESTED = 0.6;
const PRE_CHECKED_THRESHOLD = 0.8;

// src/db/operations.ts EXTEND:
export async function bulkLinkSchools(stichtingId: string, schoolIds: string[]): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Lib — Inline Levenshtein + stichtingMatcher + unit tests</name>
  <read_first>
    - apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md (§ Standard Stack + § Code Example 2)
    - apps/concurrentoolVO/src/lib/ (lijst voor pattern consistency)
    - apps/concurrentoolVO/src/lib/__tests__/stichtingMatcher.test.ts (Plan 01 scaffold)
  </read_first>
  <files>src/lib/stringSimilarity.ts, src/lib/stichtingMatcher.ts, src/lib/__tests__/stringSimilarity.test.ts, src/lib/__tests__/stichtingMatcher.test.ts</files>
  <action>
    1. src/lib/stringSimilarity.ts: kopieer Wagner-Fischer impl uit RESEARCH § Standard Stack. Pure functie. Public domain JSDoc. ~50 LOC.
    2. src/lib/stichtingMatcher.ts: implementeer suggestSchoolsForStichting() per RESEARCH § Code Example 2:
       - Filter schools waar stichtingId === null
       - Per school: nameSim = similarity(stichtingName.lowercase().trim(), schoolName.lowercase().trim())
       - score = nameSim * NAAM_WEIGHT
       - Als beide region's case-insensitive match: score += REGIO_WEIGHT, reasons.push('regio-match')
       - Filter score >= 0.6
       - Sort descending
       - preChecked = score > 0.8
    3. src/lib/__tests__/stringSimilarity.test.ts:
       - returns 1 for identical strings
       - returns 0 for completely different
       - handles empty strings
       - case-sensitive (caller lowercases)
    4. src/lib/__tests__/stichtingMatcher.test.ts: vervang it.todo's:
       - empty array when no ongekoppelde schools
       - exact match score=1 preChecked=true
       - regio-match adds 0.35
       - filters scores below 0.6
       - missing region handled gracefully (no NaN)
       - sorts descending by score
       - excludes schools waar stichtingId !== null
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npx vitest run src/lib/__tests__/stringSimilarity.test.ts src/lib/__tests__/stichtingMatcher.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - similarity(a, a) === 1 voor elke string
    - suggestSchoolsForStichting returnt correct gesorteerd array
    - preChecked true alleen bij score > 0.8
    - Schools met stichtingId !== null uitgefilterd
    - Geen NaN bij ontbrekende regio
    - 11 tests groen (4 stringSimilarity + 7 stichtingMatcher)
    - npm run build slaagt
  </acceptance_criteria>
  <done>Smart-suggestion lib functioneel + getest. Geen npm dep (Phase 22 policy).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UI — BulkLinkSchoolsDialog + StichtingSuggestionList + StichtingDetailPage integratie + bulkLinkSchools operation</name>
  <read_first>
    - apps/concurrentoolVO/src/db/operations.ts (post-Plan 02 — heeft linkSchoolToStichting al)
    - apps/concurrentoolVO/src/features/stichtingen/StichtingDetailPage.tsx (Plan 02 leverde basis)
    - apps/concurrentoolVO/src/components/ui/ (modal/dialog components)
  </read_first>
  <files>src/db/operations.ts, src/features/stichtingen/components/StichtingSuggestionList.tsx, src/features/stichtingen/components/BulkLinkSchoolsDialog.tsx, src/features/stichtingen/StichtingDetailPage.tsx, src/features/stichtingen/hooks/useStichtingen.ts</files>
  <action>
    1. src/db/operations.ts toevoegen:
       ```
       export async function bulkLinkSchools(stichtingId: string, schoolIds: string[]): Promise<void> {
         if (schoolIds.length === 0) return;
         await supabase.from('schools').update({ stichting_id: stichtingId }).in('id', schoolIds);
         // Dexie bulk update
       }
       ```
       Plus optionele bulkUnlinkSchools voor symmetrie.
    2. StichtingSuggestionList.tsx: stateless. Props { suggestions, selectedIds (Set), onToggle }. Renders lijst met checkbox + naam + score-badge + reasons-tooltip. Pre-checked badges visueel onderscheidend. Empty: "Geen automatische suggesties. Voeg handmatig scholen toe hieronder."
    3. BulkLinkSchoolsDialog.tsx: stateful. Props { stichting, ongekoppeldeSchools, open, onClose, onLinked }. Layout:
       - Header: "Scholen koppelen aan {stichting.name}"
       - Sectie 1: "Voorgestelde matches" — <StichtingSuggestionList> met output van suggestSchoolsForStichting. Initial selectedIds = pre-checked items.
       - Sectie 2: "Handmatig toevoegen" — collapsible. Schools niet in suggestions.
       - Footer: "{N} scholen geselecteerd" + Annuleren + Koppelen (disabled als selectedIds.size === 0)
       - Submit: await bulkLinkSchools(stichting.id, Array.from(selectedIds)) → invalidateQueries → onLinked → close.
    4. StichtingDetailPage.tsx uitbreiden:
       - Scholen-tab krijgt "+ Scholen koppelen" knop bovenaan. Klik opent BulkLinkSchoolsDialog.
       - Per school-rij: "Loskoppelen" knop → unlinkSchoolFromStichting + invalidateQueries.
       - Ongekoppelde-schools-data via nieuwe hook useUnlinkedSchools.
    5. src/features/stichtingen/hooks/useStichtingen.ts: voeg useUnlinkedSchools query hook toe.
    6. UI Dutch. Confidence-percentage in suggesties zichtbaar (recommendation: ja, transparantie voor sales).
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - StichtingDetailPage Scholen-tab "+ Scholen koppelen" knop
    - Dialog toont voorgestelde matches met confidence
    - Suggesties score > 0.8 default checked
    - Handmatig section toont overige ongekoppelde schools
    - Bulk-koppel zet stichting_id op N schools in 1 call
    - Loskoppel per rij werkt
    - npm run build slaagt
    - Bestaande tests blijven groen
  </acceptance_criteria>
  <done>R11 compleet end-to-end.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Browser → Supabase | Bulk UPDATE in 1 transactie |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-01 | DoS | Levenshtein O(N×M) bij superlange namen | mitigate | Schools.name max 100 chars (Plan 02). N=50, M=100 → ≤500K ops. Geen risk. |
| T-27-07-02 | Tampering | Race-condition: 2 users koppelen zelfde school | accept | Supabase laatste-schrijver-wint via updated_at |
| T-27-07-03 | Tampering | Cross-team bulk-link | mitigate | Bestaande RLS schools UPDATE policy blokkeert cross-team writes |
| T-27-07-04 | Information Disclosure | Suggestion-scores impliciete data leak | accept | Alle data al team-scoped via RLS |
</threat_model>

<verification>
- npm run build groen
- npx vitest run stringSimilarity + stichtingMatcher groen (11 tests)
- Manual smoke: /stichtingen/:id → Scholen-tab → "+ Scholen koppelen" → suggesties → kies 3 → Koppelen → 3 schools verschijnen → Loskoppel 1 → 2 blijven
</verification>

<success_criteria>
- R11 acceptance: 10 schools ongekoppeld zichtbaar; multi-select 3 + bulk-koppel werkt; loskoppel werkt
- Smart-suggestion levert pre-checked items
- Geen npm pkg toegevoegd
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-07-SUMMARY.md`
</output>
