---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 14
type: execute
wave: 6
depends_on: ["27-13"]
files_modified:
  - src/models/school.ts
  - src/router/routes.ts
  - src/features/school-profile/tabs/ComparisonTab.tsx
  - src/features/price-comparison/wizard/scenario-detection.ts
  - src/features/price-comparison/wizard/__tests__/scenario-detection.test.ts
  - src/features/price-comparison/wizard/ComparisonWizard.tsx
  - src/engine/scenario-detection.ts
  - src/engine/__tests__/scenario-detection.test.ts
autonomous: true
requirements: [R10]
tags: [cito-oud-cleanup-phase-3, scenario-rename, routes-cleanup, scenario-detection-rewrite]

must_haves:
  truths:
    - "Scenario type hernoemd naar 'basis' | 'plus' | 'upsell'"
    - "SCENARIO_LABELS map hernoemd met Dutch labels"
    - "Routes /migratie + /huidig-vs-cito verwijderd uit routes.ts"
    - "routeTree.addChildren bevat geen migratieRoute/huidigVsCitoRoute meer"
    - "SCHOOL_TAB_ROUTES heeft geen .migratie + .huidig-vs-cito"
    - "ComparisonTab verwijst niet naar MigrationPage/CurrentVsProposedPage imports"
    - "scenario-detection rewritten zonder migrationModuleIds detection"
    - "grep -rn 'A' \\| 'B' \\| 'C' (Scenario literals) levert 0 hits in src/"
  artifacts:
    - path: "src/models/school.ts"
      provides: "Scenario type = 'basis' | 'plus' | 'upsell'"
      contains: "Scenario"
    - path: "src/router/routes.ts"
      provides: "Routes zonder migratieRoute + huidigVsCitoRoute"
      contains: "routeTree"
  key_links:
    - from: "src/router/routes.ts"
      to: "(removed routes)"
      via: "GEEN imports meer naar MigrationPage / CurrentVsProposedPage"
      pattern: "migratieRoute|huidigVsCitoRoute"
    - from: "src/engine/scenario-detection.ts"
      to: "src/models/school.ts (Scenario)"
      via: "scenario type input/output basis/plus/upsell"
      pattern: "Scenario"
---

<objective>
Implementeer R10 cleanup-fase 3 (van 4): Scenario type rename + routes cleanup + scenario-detection rewrites. Plan 27-15 voegt calculateBasisPlusUpsell() + WizardStep5 Basis/Plus/Upsell + Supabase data-migration toe.

Purpose: Plan 27-13 verwijderde cito-oud enum-references. Deze plan hernoemt Scenario 'A'|'B'|'C' → 'basis'|'plus'|'upsell', ruimt /migratie + /huidig-vs-cito routes op, en rewrite scenario-detection logic die nu zonder cito-oud + migration moet werken.

Output: 8 files updated. Scenario-detection herzien voor basis/plus/upsell selectie op basis van customerType (Plan 27-09 leverde customerType via Plan 27-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/src/models/school.ts
@apps/concurrentoolVO/src/router/routes.ts
@apps/concurrentoolVO/src/engine/scenario-detection.ts

<interfaces>
Doelstaat na deze plan:
```
// src/models/school.ts
export type Scenario = 'basis' | 'plus' | 'upsell';
export const SCENARIO_LABELS: Record<Scenario, string> = {
  basis: 'Cito Basis vs concurrent',
  plus: 'Cito Plus vs concurrent',
  upsell: 'Upsell: Cito Basis naar Cito Plus',
};

// src/router/routes.ts
// VERWIJDERD: migratieRoute, huidigVsCitoRoute
// VERWIJDERD: SCHOOL_TAB_ROUTES.migratie + .huidig-vs-cito
// Bestaande routes (schoolRoute, prijzenRoute, stichtingen routes — Plan 27-02) blijven

// src/engine/scenario-detection.ts (REWRITE)
// VERWIJDERD: migrationModuleIds detection logic
// BEHOUDEN: COMPETITOR_PROVIDERS set
// NIEUW: recommendedScenario(customerType, hasCompetitorModules) → Scenario
//   - customerType='nieuwe-prospect' + hasCompetitor → 'basis' (vergelijking met concurrent)
//   - customerType='huidige-cito' + heeft Basis bundle → 'upsell' (Basis→Plus pitch)
//   - default → 'basis'
```

Scenario rename touches consumers:
- src/models/school.ts (type + labels)
- src/engine/scenario-detection.ts (recommendedScenario rewrite)
- src/features/price-comparison/wizard/scenario-detection.ts (mogelijk delete-if-empty)
- src/features/price-comparison/wizard/ComparisonWizard.tsx (mogelijk delete-if-only-scenario-B-C)
- src/features/school-profile/tabs/ComparisonTab.tsx (verwijder MigrationPage + CurrentVsProposedPage imports + ViewSwitcher)

Plan 27-15 doet WizardStep5 rewrite met nieuwe scenario-id's en calculateBasisPlusUpsell wiring.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scenario type rename in school.ts + ComparisonTab routing cleanup + routes.ts cleanup</name>
  <read_first>
    - apps/concurrentoolVO/src/models/school.ts (Scenario type + SCENARIO_LABELS)
    - apps/concurrentoolVO/src/router/routes.ts (full file — migratieRoute lines + huidigVsCitoRoute lines + routeTree.addChildren + SCHOOL_TAB_ROUTES)
    - apps/concurrentoolVO/src/features/school-profile/tabs/ComparisonTab.tsx (full file — Scenario-B/C routing, ViewSwitcher, MigrationPage import, CurrentVsProposedPage import)
  </read_first>
  <files>src/models/school.ts, src/router/routes.ts, src/features/school-profile/tabs/ComparisonTab.tsx</files>
  <action>
    1. src/models/school.ts:
       - Hernoem `export type Scenario = 'A' | 'B' | 'C'` → `'basis' | 'plus' | 'upsell'`
       - Update SCENARIO_LABELS:
         ```
         export const SCENARIO_LABELS: Record<Scenario, string> = {
           basis: 'Cito Basis vs concurrent',
           plus: 'Cito Plus vs concurrent',
           upsell: 'Upsell: Cito Basis naar Cito Plus',
         };
         ```
    2. src/router/routes.ts:
       - DELETE migratieRoute declaration (lines ~107-114 per RESEARCH)
       - DELETE huidigVsCitoRoute declaration (lines ~97-104)
       - Verwijder beide uit `routeTree.addChildren([...])`
       - Verwijder uit `SCHOOL_TAB_ROUTES` map: `migratie` + `huidig-vs-cito` keys
       - Verwijder imports die alleen door deze routes werden gebruikt
    3. src/features/school-profile/tabs/ComparisonTab.tsx:
       - Verwijder import MigrationPage en CurrentVsProposedPage
       - Verwijder Scenario-B/Scenario-C routing-paths (was 'B'|'C')
       - ViewSwitcher voor B-C verwijderen
       - Behoud Scenario-A path (gewone comparison) — vervang 'A' string met 'basis' indien gehardcoded
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && grep -rn "migratieRoute\|huidigVsCitoRoute" src/ | head -5</automated>
    <automated>cd apps/concurrentoolVO && grep -rn "Scenario = 'A'\|MigrationPage\|CurrentVsProposedPage" src/features/school-profile/tabs/ComparisonTab.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Scenario type = 'basis'|'plus'|'upsell' in school.ts
    - SCENARIO_LABELS heeft 3 Dutch labels
    - routes.ts heeft geen migratieRoute / huidigVsCitoRoute meer
    - SCHOOL_TAB_ROUTES heeft geen migratie / huidig-vs-cito keys
    - ComparisonTab importeert MigrationPage + CurrentVsProposedPage niet meer
    - grep migratieRoute|huidigVsCitoRoute in src/ levert 0 hits
  </acceptance_criteria>
  <done>Type + routes cleanup compleet. Task 2 rewrite scenario-detection logic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scenario-detection rewrites (engine + wizard) + ComparisonWizard cleanup + tests update</name>
  <read_first>
    - apps/concurrentoolVO/src/engine/scenario-detection.ts (FULL FILE — kritische rewrite)
    - apps/concurrentoolVO/src/engine/__tests__/scenario-detection.test.ts (heavy rewrite per RESEARCH 14 hits)
    - apps/concurrentoolVO/src/features/price-comparison/wizard/scenario-detection.ts (mogelijk hele file deletable)
    - apps/concurrentoolVO/src/features/price-comparison/wizard/__tests__/scenario-detection.test.ts (heavy update per RESEARCH 7 hits)
    - apps/concurrentoolVO/src/features/price-comparison/wizard/ComparisonWizard.tsx (mogelijk delete als alleen Scenario B/C dient)
  </read_first>
  <files>src/engine/scenario-detection.ts, src/engine/__tests__/scenario-detection.test.ts, src/features/price-comparison/wizard/scenario-detection.ts, src/features/price-comparison/wizard/__tests__/scenario-detection.test.ts, src/features/price-comparison/wizard/ComparisonWizard.tsx</files>
  <action>
    1. src/engine/scenario-detection.ts — REWRITE:
       - BEHOUDEN: `COMPETITOR_PROVIDERS` set (filter functionality nodig elders)
       - VERWIJDEREN: migrationModuleIds detection block (alle logic die naar oude scenario B mapte)
       - HERZIEN: recommendedScenario(customerType: CustomerType, hasCompetitorModules: boolean) → Scenario:
         ```
         export function recommendedScenario(
           customerType?: 'huidige-cito' | 'nieuwe-prospect' | 'gedeeltelijk',
           hasCompetitorModules: boolean,
         ): Scenario {
           if (customerType === 'nieuwe-prospect') return 'basis'; // pure acquisitie-pitch
           if (customerType === 'huidige-cito' && !hasCompetitorModules) return 'upsell';
           // gedeeltelijk / default
           return 'basis';
         }
         ```
       - Verwijder oude logic die scenario === 'A'|'B'|'C' produceerde
    2. src/engine/__tests__/scenario-detection.test.ts — REWRITE (14 hits per RESEARCH):
       - Vervang tests die scenario 'A'|'B'|'C' verwachtten met scenario 'basis'|'plus'|'upsell'
       - Nieuwe tests:
         - 'recommendedScenario returns basis for nieuwe-prospect'
         - 'recommendedScenario returns upsell for huidige-cito without competitor modules'
         - 'recommendedScenario returns basis for gedeeltelijk customerType'
         - 'COMPETITOR_PROVIDERS set bevat dia + jij + overig (geen cito varianten)'
    3. src/features/price-comparison/wizard/scenario-detection.ts:
       - Lezen om te zien of file ALLEEN scenario-B/C logic bevat. Zo ja: DELETE.
       - Zo nee: strip de B/C-specifieke branches en behoud relevante helpers.
    4. src/features/price-comparison/wizard/__tests__/scenario-detection.test.ts:
       - Als source-file deleted: DELETE test
       - Anders: heavy update (7 hits per RESEARCH) — vervang scenario literals
    5. src/features/price-comparison/wizard/ComparisonWizard.tsx:
       - Lezen om te zien of component scenario-B/C ondersteunde
       - Als hele component alleen scenario B/C diende: DELETE
       - Anders: strip B/C branches
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && grep -rn "'A' | 'B' | 'C'\|scenario === 'A'\|scenario === 'B'\|scenario === 'C'" src/ | head -10</automated>
    <automated>cd apps/concurrentoolVO && npx vitest run src/engine/__tests__/scenario-detection.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep voor scenario A/B/C literals in src/ levert 0 hits
    - engine/scenario-detection.ts rewritten met recommendedScenario(customerType, hasCompetitorModules) → 'basis'|'plus'|'upsell'
    - COMPETITOR_PROVIDERS set behouden
    - engine/__tests__/scenario-detection.test.ts groen
    - wizard/scenario-detection.ts + ComparisonWizard.tsx + bijhorende tests geupdate-of-deleted
  </acceptance_criteria>
  <done>Scenario-detection cleanup compleet. Plan 27-15 doet calculateBasisPlusUpsell + WizardStep5 + final engine cleanup.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| (internal refactor) | Geen runtime trust boundary changes |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-14-01 | Tampering | Scenario rename mist een referentie → runtime error | mitigate | grep-gate in verify levert 0 hits voor scenario A/B/C literals; Plan 27-15 doet full build |
| T-27-14-02 | Tampering | Route delete laat dead-imports achter | mitigate | TS-compiler detecteert dead imports in Plan 27-15 build-gate |
</threat_model>

<verification>
- grep scenario A/B/C literals 0 hits
- grep migratieRoute/huidigVsCitoRoute 0 hits
- engine/scenario-detection.test.ts groen
</verification>

<success_criteria>
- Scenario type hernoemd
- Routes opgeruimd
- Scenario-detection logic werkt met customerType + hasCompetitorModules → 'basis'|'plus'|'upsell'
- Plan 27-15 kan op clean type-system bouwen
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-14-SUMMARY.md`
</output>
