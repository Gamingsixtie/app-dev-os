---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 08
type: execute
wave: 4
depends_on: ["27-01", "27-03", "27-05"]
files_modified:
  - src/models/school.ts
  - src/db/types.ts
  - src/db/database.ts
  - supabase/migrations/017_competitor_context.sql
  - src/features/school-profile/schemas/competitor-context.schema.ts
  - src/features/school-profile/schemas/step4-schema.ts
  - src/features/school-profile/components/TimeInputSection.tsx
  - src/features/school-profile/components/TimeSavingsSection.tsx
  - src/features/school-profile/components/WizardSummaryBlock.tsx
  - src/features/school-profile/components/WizardStep4.tsx
  - src/features/school-profile/store.ts
  - src/features/school-profile/components/__tests__/WizardStep4.test.tsx
  - src/features/school-profile/components/__tests__/TimeInputSection.test.tsx
autonomous: true
requirements: [R8]
tags: [wizard-step4, summary-block, opmerkingen, time-input, time-savings-refactor, dual-mode]

must_haves:
  truths:
    - "WizardStep4 toont bovenaan een read-only summary van Stap 1-3 met 'Wijzig'-link per blok"
    - "WizardStep4 toont opmerkingen-textarea (max 1000 chars) met label 'Wat zijn pijnpunten?'"
    - "WizardStep4 toont TimeInputSection in mode=competitor-time onder opmerkingen"
    - "TimeSavingsSection refactored naar TimeInputSection met mode-prop (D-19)"
    - "Cito-tijdwinst flow blijft werken via mode=cito-savings"
    - "competitorRemarks + competitorTimeMinutes persisteren in Dexie + Supabase"
    - "'Wijzig'-knop in summary navigeert terug via setStep()"
  artifacts:
    - path: "src/features/school-profile/components/TimeInputSection.tsx"
      provides: "Dual-mode tijd-input (D-19)"
      min_lines: 60
    - path: "src/features/school-profile/components/WizardSummaryBlock.tsx"
      provides: "Read-only summary met Wijzig-knoppen (D-20)"
      min_lines: 50
    - path: "src/features/school-profile/schemas/competitor-context.schema.ts"
      provides: "Zod schema competitorRemarks + competitorTimeMinutes (D-18)"
      min_lines: 15
    - path: "supabase/migrations/017_competitor_context.sql"
      provides: "ALTER schools competitor_remarks + competitor_time_minutes"
      contains: "competitor_remarks"
  key_links:
    - from: "src/features/school-profile/components/WizardStep4.tsx"
      to: "src/features/school-profile/components/WizardSummaryBlock.tsx"
      via: "JSX render bovenaan"
      pattern: "WizardSummaryBlock"
    - from: "src/features/school-profile/components/WizardSummaryBlock.tsx"
      to: "src/features/school-profile/store.ts (setCurrentStep)"
      via: "Wijzig-knop callback"
      pattern: "setCurrentStep"
    - from: "src/features/school-profile/components/WizardStep4.tsx"
      to: "src/features/school-profile/components/TimeInputSection.tsx"
      via: "render mode=competitor-time"
      pattern: "mode=['\"]competitor-time['\"]"
---

<objective>
Implementeer R8: WizardStep4 krijgt 3 nieuwe secties: (a) read-only WizardSummaryBlock van Stap 1-3 met 'Wijzig' per blok, (b) opmerkingen-textarea voor pijnpunten, (c) TimeInputSection mode=competitor-time voor concurrent-tijd. Tegelijk refactor TimeSavingsSection → TimeInputSection met mode-prop (D-19).

Purpose: Sales legt pijnpunten + concurrent-tijdverlies vast. Plan 09 (R9) gebruikt remarks voor AI-matching. WizardSummaryBlock voorkomt context-loss.

Output: WizardStep4 herstructureerd. TimeSavingsSection → TimeInputSection dual-mode. WizardSummaryBlock nieuw. Cito-oud blijft in BASE_PROVIDER_OPTIONS (Plan 10 ruimt op).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-CONTEXT.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/src/features/school-profile/components/WizardStep4.tsx
@apps/concurrentoolVO/src/features/school-profile/components/TimeSavingsSection.tsx
@apps/concurrentoolVO/src/features/school-profile/schemas/step4-schema.ts
@apps/concurrentoolVO/src/models/school.ts
@apps/concurrentoolVO/src/models/time-savings.ts

<interfaces>
TimeInputSection props (D-19):
```
interface TimeInputSectionProps {
  mode: 'cito-savings' | 'competitor-time';
  value: Record<TaskType, number>;
  onChange: (taskType, minutes) => void;
  title?: string;
  description?: string;
}
```

cito-savings mode (existing): 5 taken (rechten/resetten/inloggen/planning/koppeling), label "Hoeveel uur per week...", schrijft naar timeSavings store.

competitor-time mode (NEW): 5-6 taken (+ rapporten-lezen optioneel), label "Hoeveel minuten/week kost X bij huidige aanbieder?", schrijft naar competitorTimeMinutes store.

WizardSummaryBlock blokken (D-20):
1. School-blok: schoolName + levels + schoolType + customSchoolType + customerType + growthTrajectory → setCurrentStep(0)
2. Leerlingen-blok: studentCounts samenvatting + currentToolUsage per niveau → setCurrentStep(1)
3. Modules-blok: lijst gegroepeerd per category → setCurrentStep(2)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Data-laag + TimeSavingsSection → TimeInputSection refactor (D-19)</name>
  <read_first>
    - apps/concurrentoolVO/src/features/school-profile/components/TimeSavingsSection.tsx (current single-mode)
    - apps/concurrentoolVO/src/models/time-savings.ts (Plan 01 relocated types)
    - apps/concurrentoolVO/src/models/school.ts (post-Plan 03/05)
    - apps/concurrentoolVO/src/features/school-profile/store.ts (existing actions)
  </read_first>
  <files>src/models/school.ts, src/db/types.ts, src/db/database.ts, supabase/migrations/017_competitor_context.sql, src/features/school-profile/components/TimeInputSection.tsx, src/features/school-profile/components/TimeSavingsSection.tsx, src/features/school-profile/store.ts, src/features/school-profile/components/__tests__/TimeInputSection.test.tsx</files>
  <action>
    1. src/models/school.ts uitbreiden:
       ```
       competitorRemarks?: string;  // max 1000
       competitorTimeMinutes?: Partial<Record<TaskType, number>>;  // TaskType = 'rechten'|'resetten'|'inloggen'|'planning'|'koppeling'|'rapporten-lezen'
       ```
       Plan 09 voegt later painPointMatchFeedback toe.
    2. src/db/types.ts: DexieSchoolRecord krijgt zelfde 2 velden.
    3. src/db/database.ts: append in version(3).upgrade()-callback:
       ```
       school.competitorRemarks = school.competitorRemarks ?? '';
       school.competitorTimeMinutes = school.competitorTimeMinutes ?? {};
       ```
    4. supabase/migrations/017_competitor_context.sql:
       ```
       ALTER TABLE schools
         ADD COLUMN competitor_remarks TEXT NOT NULL DEFAULT '',
         ADD COLUMN competitor_time_minutes JSONB NOT NULL DEFAULT '{}'::jsonb;
       ```
    5. store.ts actions: setCompetitorRemarks(text), setCompetitorTime(taskType, minutes).
    6. TimeInputSection.tsx (NEW): kopieer logica TimeSavingsSection en parametriseer per Interfaces-block. Mode-conditional headers + labels. competitor-time mode: 6 taken (5 existing + rapporten-lezen). Beide modes hergebruiken TIME_SAVING_TASKS uit @/models/time-savings.
    7. TimeSavingsSection.tsx: refactor tot 1-line wrapper die `<TimeInputSection mode="cito-savings" {...props} />` rendert. Backward-compat — bestaande WaardeTab consumers werken zonder wijziging.
    8. TimeInputSection.test.tsx (NEW):
       - renders cito-savings mode 5 tasks + savings-labels
       - renders competitor-time mode 6 tasks + time-cost labels
       - persists value via onChange
       - mode switch changes header text
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10 && npx vitest run src/features/school-profile/components/__tests__/TimeInputSection.test.tsx 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - SchoolRecord + DexieSchoolRecord + Supabase migratie hebben 2 nieuwe velden
    - Dexie v3 upgrade idempotent
    - Store heeft setCompetitorRemarks + setCompetitorTime
    - TimeInputSection rendert beide modes correct
    - TimeSavingsSection wrapper-pattern werkt (bestaande WaardeTab tests groen)
    - TimeInputSection.test.tsx groen
    - npm run build slaagt
  </acceptance_criteria>
  <done>Data-laag + TimeInputSection dual-mode klaar.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: WizardSummaryBlock + WizardStep4 integratie + Zod schemas + tests</name>
  <read_first>
    - apps/concurrentoolVO/src/features/school-profile/components/WizardStep4.tsx (current shape met provider-dropdown + price-input)
    - apps/concurrentoolVO/src/features/school-profile/schemas/step4-schema.ts
    - apps/concurrentoolVO/src/features/school-profile/components/__tests__/WizardStep4.test.tsx
    - apps/concurrentoolVO/src/features/school-profile/store.ts (post-Plan 03/05 — alle wizard-getters beschikbaar)
  </read_first>
  <files>src/features/school-profile/schemas/competitor-context.schema.ts, src/features/school-profile/schemas/step4-schema.ts, src/features/school-profile/components/WizardSummaryBlock.tsx, src/features/school-profile/components/WizardStep4.tsx, src/features/school-profile/components/__tests__/WizardStep4.test.tsx</files>
  <action>
    1. competitor-context.schema.ts (NEW, D-18):
       ```
       import { z } from 'zod';
       export const taskTypeEnum = z.enum(['rechten','resetten','inloggen','planning','koppeling','rapporten-lezen']);
       export const competitorContextSchema = z.object({
         competitorRemarks: z.string().max(1000, 'Maximaal 1000 tekens').default(''),
         competitorTimeMinutes: z.record(taskTypeEnum, z.number().min(0).max(600)).default({}),
       });
       ```
    2. step4-schema.ts: .merge(competitorContextSchema) op bestaand.
    3. WizardSummaryBlock.tsx (NEW, D-17, D-20):
       - Props { schoolData, onEdit }
       - 3 sub-blokken (School / Leerlingen / Modules) per Interfaces-block
       - Per blok: heading + read-only display + "Wijzig"-knop rechtsboven (calls onEdit(stepIndex))
       - Gebruik bestaande label-constants
       - Modules-blok: gegroepeerd per category
       - Empty state per blok: "Nog niet ingevuld"
    4. WizardStep4.tsx audit per D-16:
       - Behoud provider-dropdown + price-input + custom-naam input + publicatieprijs + warnings + DifferentiatorComparison
       - NIET BASE_PROVIDER_OPTIONS aanraken (cito-oud blijft tot Plan 10)
       - Voeg bovenaan toe:
         ```
         <WizardSummaryBlock schoolData={schoolDataFromStore} onEdit={(step) => useSchoolProfileStore.getState().setCurrentStep(step)} />
         <hr />
         ```
       - Voeg onderaan toe (na module-rijen):
         ```
         <hr />
         <section aria-labelledby="opmerkingen-heading">
           <h2 id="opmerkingen-heading">Pijnpunten bij huidig pakket</h2>
           <textarea {...register('competitorRemarks')} maxLength={1000} placeholder="Wat zijn pijnpunten bij het huidige pakket?" />
           <div className="text-sm text-gray-500">{watchedRemarks.length} / 1000</div>
         </section>
         <hr />
         <TimeInputSection mode="competitor-time" value={watch('competitorTimeMinutes') ?? {}} onChange={(task, mins) => { setValue(`competitorTimeMinutes.${task}`, mins); setCompetitorTime(task, mins); }} />
         ```
       - Submit-handler: persist via setCompetitorRemarks + setCompetitorTime store actions.
    5. WizardStep4.test.tsx toevoegen:
       - renders WizardSummaryBlock met 3 sub-blocks
       - Wijzig in school-blok calls setCurrentStep(0)
       - Wijzig in modules-blok calls setCurrentStep(2)
       - opmerkingen-textarea persists value
       - TimeInputSection competitor-time 6 task rows
       - opmerkingen max 1000 chars (Zod refusal)
       - counter toont {N} / 1000
    6. Alle UI Dutch.
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -10 && npx vitest run src/features/school-profile/components/__tests__/WizardStep4.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - WizardStep4 toont WizardSummaryBlock met 3 blokken + Wijzig-knoppen
    - Wijzig-knoppen roepen setCurrentStep(N) correct
    - Opmerkingen-textarea persisteert (max 1000)
    - TimeInputSection competitor-time mode rendert
    - Tests groen
    - npm run build slaagt
    - Cito-oud blijft in BASE_PROVIDER_OPTIONS
  </acceptance_criteria>
  <done>R8 compleet.</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Browser → Supabase | Persistence competitor_remarks (free-text 1000) + JSONB |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08-01 | Tampering | XSS-payload in competitorRemarks naar PDF | mitigate | React escapeert by default; @react-pdf rendert plain text. Zod max 1000. |
| T-27-08-02 | Tampering | competitorRemarks bevat PII (telefoonnrs, namen) | accept | Tool intern, RLS team-scoped. User trained. |
| T-27-08-03 | DoS | competitorTimeMinutes > 600/week onrealistisch | mitigate | Zod .max(600) per taak |
| T-27-08-04 | Tampering | setCurrentStep out-of-range | mitigate | TOTAL_STEPS=5 invariant; store clampt 0..4 |
</threat_model>

<verification>
- npm run build groen
- WizardStep4 + TimeInputSection tests groen
- Bestaande WaardeTab tests blijven groen (wrapper pattern)
- Manual smoke: Stap 4 → 3 blokken zichtbaar → Wijzig in school-blok navigeert Stap 1 → terug → opmerkingen + tijd-input werken → submit → terug → waardes blijven
</verification>

<success_criteria>
- R8 acceptance: alle 3 secties zichtbaar
- Summary update reactief
- Opmerkingen + tijd persisteren
- Plan 09 heeft competitorRemarks om AI-matching op te starten
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-08-SUMMARY.md`
</output>
