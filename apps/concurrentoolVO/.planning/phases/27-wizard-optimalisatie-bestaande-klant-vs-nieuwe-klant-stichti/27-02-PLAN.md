---
phase: 27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti
plan: 02
type: execute
wave: 1
depends_on: ["27-01"]
files_modified:
  - src/models/stichting.ts
  - src/db/types.ts
  - src/db/database.ts
  - src/db/operations.ts
  - supabase/migrations/014_stichtingen.sql
  - src/features/stichtingen/StichtingOverviewPage.tsx
  - src/features/stichtingen/StichtingDetailPage.tsx
  - src/features/stichtingen/components/StichtingCard.tsx
  - src/features/stichtingen/components/StichtingForm.tsx
  - src/features/stichtingen/components/StichtingDetailTabs.tsx
  - src/features/stichtingen/hooks/useStichtingen.ts
  - src/features/stichtingen/hooks/useStichting.ts
  - src/features/stichtingen/schemas/stichting.schema.ts
  - src/router/routes.ts
autonomous: true
requirements: [R1]
tags: [stichting, crud, dexie, supabase, tanstack-router]

must_haves:
  truths:
    - "User kan op /stichtingen route een card-grid zien"
    - "User kan een nieuwe stichting aanmaken via dialog (naam + regio)"
    - "User kan stichting wijzigen (naam, regio)"
    - "User kan stichting verwijderen ALLEEN als geen scholen gekoppeld (D-04)"
    - "/stichtingen/:id toont detail-view met 3 tabs (Overzicht/Scholen/Export)"
    - "Stichting-data persisteert in Supabase RLS team-scoped + Dexie spiegel"
  artifacts:
    - path: "src/models/stichting.ts"
      provides: "StichtingRecord type + UsageMix enum + label maps"
      min_lines: 30
    - path: "supabase/migrations/014_stichtingen.sql"
      provides: "stichtingen tabel + RLS + ALTER schools met stichting_id FK"
      contains: "CREATE TABLE stichtingen"
    - path: "src/db/database.ts"
      provides: "Dexie version(3) met stichtingen tabel"
      contains: "this.version(3)"
    - path: "src/features/stichtingen/StichtingOverviewPage.tsx"
      provides: "Card-grid + create-knop (D-02)"
      min_lines: 40
    - path: "src/features/stichtingen/StichtingDetailPage.tsx"
      provides: "Detail-view met 3 tabs (D-05)"
      min_lines: 40
    - path: "src/router/routes.ts"
      provides: "stichtingenRoute + stichtingDetailRoute toegevoegd"
      contains: "stichtingenRoute"
  key_links:
    - from: "src/features/stichtingen/StichtingOverviewPage.tsx"
      to: "src/features/stichtingen/hooks/useStichtingen.ts"
      via: "useStichtingen() React Query hook"
      pattern: "useStichtingen\\("
    - from: "src/features/stichtingen/hooks/useStichtingen.ts"
      to: "src/db/operations.ts"
      via: "createStichting / listStichtingen"
      pattern: "createStichting|listStichtingen"
    - from: "src/db/operations.ts"
      to: "supabase.from('stichtingen')"
      via: "Supabase CRUD"
      pattern: "from\\(['\"]stichtingen['\"]\\)"
    - from: "src/router/routes.ts"
      to: "src/features/stichtingen/StichtingOverviewPage.tsx"
      via: "lazyRouteComponent import"
      pattern: "@/features/stichtingen/StichtingOverviewPage"
---

<objective>
Implementeer R1: Stichting (bestuur) als eerste-klas entiteit. Eigen Supabase tabel + Dexie spiegel (D-01), card-grid UI op /stichtingen (D-02), delete-cascade-guard (D-04), detail-view met 3 tabs (D-05).

Purpose: Stichting wordt anker waar Plan 03 (R3/R4), Plan 07 (R11 bulk-migratie), en Plan 11 (R2 bulk-export) op steunen.

Output: Stichting entity + CRUD + UI route + detail-view. GEEN bulk-link (Plan 07). GEEN export (Plan 11).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/concurrentoolVO/.planning/PROJECT.md
@apps/concurrentoolVO/.planning/STATE.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-SPEC.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-CONTEXT.md
@apps/concurrentoolVO/.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-RESEARCH.md
@apps/concurrentoolVO/AGENTS.md
@apps/concurrentoolVO/src/features/school-overview/SchoolOverview.tsx
@apps/concurrentoolVO/src/features/school-overview/SchoolCard.tsx
@apps/concurrentoolVO/src/db/database.ts
@apps/concurrentoolVO/src/db/operations.ts
@apps/concurrentoolVO/src/router/routes.ts

<interfaces>
StichtingRecord shape (D-01):
```
export interface StichtingRecord {
  id: string;        // UUID
  teamId: string;
  name: string;
  region: string;    // default ''
  createdAt: Date;
  updatedAt: Date;
  createdBy?: string;
  updatedBy?: string;
}
export type UsageMix = 'cito-only' | 'concurrent-only' | 'mixed' | 'unknown';
```

Existing patterns to mirror:
- Dexie: db.version(N).stores(...).upgrade(tx => ...) in src/db/database.ts
- Supabase RLS: helper get_user_team_id() bestaat al; copy CREATE POLICY blocks uit 008_planned_touchpoints.sql
- Routes: createRoute({ getParentRoute, path, component: lazyRouteComponent(...) }) + routeTree.addChildren([...])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Data-laag — StichtingRecord type + Supabase migratie 014 + Dexie v3 + CRUD operations</name>
  <read_first>
    - apps/concurrentoolVO/src/db/types.ts (current shape)
    - apps/concurrentoolVO/src/db/database.ts (versions + upgrade pattern)
    - apps/concurrentoolVO/src/db/operations.ts (CRUD pattern voor schools)
    - apps/concurrentoolVO/supabase/migrations/008_planned_touchpoints.sql (RLS template)
    - apps/concurrentoolVO/supabase/migrations/ (lijst — bevestig 014 is volgend vrij nummer)
  </read_first>
  <files>src/models/stichting.ts, src/db/types.ts, src/db/database.ts, src/db/operations.ts, supabase/migrations/014_stichtingen.sql, src/features/stichtingen/schemas/stichting.schema.ts</files>
  <action>
    1. src/models/stichting.ts: StichtingRecord interface, UsageMix type, STICHTING_USAGE_MIX_LABELS (Dutch), helper getStichtingUsageMix(schools) — initieel returnt 'unknown' tot Plan 05 currentToolUsage vult.
    2. src/db/types.ts: add Stichting Dexie type + optional stichtingId on DexieSchoolRecord.
    3. src/db/database.ts: add this.version(3).stores({ schools: '++id, slug, name, updatedAt, pipelineStatus, stichtingId', stichtingen: 'id, name, region, updatedAt' }).upgrade(tx => { school.stichtingId = school.stichtingId ?? null; }). Idempotent. NIET cito-oud filter hier (Plan 10).
    4. supabase/migrations/014_stichtingen.sql:
       - CREATE TABLE stichtingen (id UUID PK, team_id UUID FK, name TEXT, region TEXT DEFAULT '', created_by, updated_by, created_at, updated_at)
       - ALTER TABLE schools ADD COLUMN stichting_id UUID REFERENCES stichtingen(id) ON DELETE SET NULL
       - NIET de andere 8 kolommen (customer_type/school_type/etc) — die schrijft Plan 03/05/08/09. Strict tot Stichting.
       - CREATE INDEX idx_schools_stichting_id, idx_stichtingen_team_id
       - 4 RLS policies (SELECT/INSERT/UPDATE/DELETE) met team_id = get_user_team_id()
       - CREATE TRIGGER set_updated_at
    5. src/db/operations.ts toevoegen:
       - createStichting(input) — Supabase insert + Dexie put
       - listStichtingen() — Supabase select + Dexie fallback
       - getStichting(id)
       - updateStichting(id, patch)
       - deleteStichting(id) — EERST count linked schools; als > 0 throw StichtingCascadeError (D-04)
       - listSchoolsForStichting(stichtingId)
       - unlinkSchoolFromStichting(schoolId)
       - linkSchoolToStichting(schoolId, stichtingId)
       Pattern spiegelen op createSchool etc. Gebruik offlineQueueIfNeeded pattern (Phase 12).
    6. src/features/stichtingen/schemas/stichting.schema.ts: Zod stichtingFormSchema (name min 2 max 100, region max 100 default ''). Dutch error messages.
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - src/models/stichting.ts exporteert StichtingRecord + UsageMix + label maps
    - supabase/migrations/014 bevat CREATE TABLE + 4 RLS policies + ALTER schools
    - Dexie version(3) registreert stichtingen tabel
    - operations.ts exporteert 7 nieuwe functies incl. deleteStichting cascade-guard
    - deleteStichting throws bij gekoppelde schools
    - npm run build slaagt
  </acceptance_criteria>
  <done>Data-laag complete. Stichting CRUD werkt programmatisch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: UI — StichtingOverviewPage + StichtingDetailPage + routes + React Query hooks</name>
  <read_first>
    - apps/concurrentoolVO/src/features/school-overview/SchoolOverview.tsx (card-grid pattern)
    - apps/concurrentoolVO/src/features/school-overview/SchoolCard.tsx (card layout)
    - apps/concurrentoolVO/src/features/school-profile/components/TabNavigation.tsx (D-05 hergebruik)
    - apps/concurrentoolVO/src/router/routes.ts (lazyRouteComponent pattern)
  </read_first>
  <files>src/features/stichtingen/StichtingOverviewPage.tsx, src/features/stichtingen/StichtingDetailPage.tsx, src/features/stichtingen/components/StichtingCard.tsx, src/features/stichtingen/components/StichtingForm.tsx, src/features/stichtingen/components/StichtingDetailTabs.tsx, src/features/stichtingen/hooks/useStichtingen.ts, src/features/stichtingen/hooks/useStichting.ts, src/router/routes.ts</files>
  <action>
    1. Hooks (useStichtingen.ts, useStichting.ts): React Query hooks met queryKey ['stichtingen'] + ['stichtingen', id]. Mutation hooks (useCreate/Update/Delete) met invalidateQueries.
    2. StichtingCard.tsx: mirror SchoolCard. Toont naam, regio, aantal-scholen-badge, mix-indicator (3 dots, initieel 'unknown' grijs tot Plan 05). Click navigeert /stichtingen/:id.
    3. StichtingForm.tsx: react-hook-form + zodResolver(stichtingFormSchema). Velden naam + regio. Submit-knop "Aanmaken" of "Opslaan". Dutch labels: "Naam stichting", "Regio (optioneel)".
    4. StichtingOverviewPage.tsx: header met "+ Nieuwe stichting" knop opent dialog met StichtingForm. CSS grid 3-col responsive (mirror SchoolOverview). Empty state Dutch. GEEN smart-suggestion hier (Plan 07).
    5. StichtingDetailTabs.tsx: hergebruik bestaande TabNavigation component. 3 tabs:
       - "Overzicht" (basis-info + counts)
       - "Scholen" (table-view linked schools met loskoppel per rij — Plan 07 vult bulk-link)
       - "Export" (placeholder "Komt in Plan 27-11")
    6. StichtingDetailPage.tsx: header met stichting-naam + Bewerken-knop + Verwijderen-knop. Verwijder dialog: als scholen gekoppeld toon "Eerst N scholen loskoppelen voordat stichting verwijderd kan worden" (D-04). Body: StichtingDetailTabs.
    7. src/router/routes.ts: voeg toe per RESEARCH Pattern 1:
       - stichtingenRoute (path /stichtingen, lazy component StichtingOverviewPage)
       - stichtingDetailRoute (path /stichtingen/$id, beforeLoad checkt exists)
       - routeTree.addChildren([..., stichtingenRoute, stichtingDetailRoute])
       NIET /migratie + /huidig-vs-cito verwijderen (Plan 10).
    8. UI Nederlands, code Engels.
  </action>
  <verify>
    <automated>cd apps/concurrentoolVO && npm run build 2>&1 | tail -15 && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Route /stichtingen rendert card-grid (leeg OK)
    - Route /stichtingen/:id rendert detail met 3 tabs
    - StichtingForm Zod-validatie + Dutch errors
    - Delete-dialog blocked bij gekoppelde schools (D-04)
    - npm run build slaagt
    - Bestaande tests blijven groen
  </acceptance_criteria>
  <done>Stichting feature volledig wired (zonder bulk-link en zonder export — Plan 07 en 11).</done>
</task>

</tasks>

<threat_model>
| Boundary | Description |
|----------|-------------|
| Browser → Supabase API | Stichting CRUD via JWT |
| Supabase API → Postgres | RLS team-scoped |

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Information Disclosure | Cross-team stichting leak | mitigate | RLS team_id = get_user_team_id() (pattern uit 008_planned_touchpoints.sql) |
| T-27-02-02 | Tampering | Accidentele delete stichting met scholen | mitigate | D-04 cascade-guard in deleteStichting throws StichtingCascadeError |
| T-27-02-03 | Tampering | Race-condition parallel edits | accept | Supabase laatste-schrijver-wint via updated_at — acceptable MVP |
| T-27-02-04 | Information Disclosure | UUIDs in /stichtingen/$id | accept | UUIDs niet predictable + RLS = safe (zelfde als /scholen/$slug) |
| T-27-02-05 | Spoofing | Manipulated team_id in insert | mitigate | RLS WITH CHECK (team_id = get_user_team_id()) op INSERT |
</threat_model>

<verification>
- npm run build groen
- npx vitest run groen
- Manual smoke: /stichtingen → grid, + Nieuwe stichting → form, submit → card verschijnt, click → detail met 3 tabs
- Manual smoke: delete leeg = werkt; delete met 1+ schools = dialog
</verification>

<success_criteria>
- D-01, D-02, D-04, D-05 geïmplementeerd
- R1 aanmaken/wijzigen/verwijderen werken
- Plan 07 + Plan 11 kunnen op deze data-laag bouwen
</success_criteria>

<output>
After completion, create `.planning/phases/27-wizard-optimalisatie-bestaande-klant-vs-nieuwe-klant-stichti/27-02-SUMMARY.md`
</output>
