# Dependency Audit Report
**Date:** 2026-05-05

## Summary

| Severity | Count | Projects Affected |
|----------|-------|-------------------|
| **HIGH** | 4 | concurrentoolVO |
| CRITICAL | 0 | — |
| **Total Blockers** | **4** | |

---

## By Project

### concurrentoolVO

| Package | Severity | Issue | Vulnerability ID | Fix Available |
|---------|----------|-------|-------------------|----------------|
| **serialize-javascript** | HIGH | RCE via RegExp.flags and Date.prototype.toISOString() | GHSA-5c6j-r48x-rmvq | ✅ Yes |
| **serialize-javascript** | MODERATE | CPU Exhaustion DoS via crafted array-like objects | GHSA-qj8w-gfj5-8c6v | ✅ Yes |
| **@rollup/plugin-terser** | HIGH | Transitive vuln via serialize-javascript | — | ✅ Yes |
| **workbox-build** | HIGH | Transitive vuln via @rollup/plugin-terser | — | ✅ Yes |
| **xlsx** | HIGH | Prototype Pollution in sheetJS | GHSA-4r6h-8v6p-xvw6 | ❌ No |
| **xlsx** | HIGH | Regular Expression Denial of Service (ReDoS) | GHSA-5pgg-2g8v-p4x9 | ❌ No |

**Details:**

- **serialize-javascript ≤7.0.4:** RCE vulnerability (CVSS 8.1) allows remote code execution via malformed RegExp.flags and Date.prototype.toISOString() serialization. Affects @rollup/plugin-terser → workbox-build dependency chain.
- **xlsx (any version):** Two unfixed HIGH vulnerabilities — Prototype Pollution (CVSS 7.8, local/user-triggered) and ReDoS (CVSS 7.5, network-triggered DoS). Direct dependency with no available fix versions.

---

## Action Items

### Fixable (Transitive Chain)

These can be resolved by upgrading workbox-build or regenerating lock file:

```bash
cd apps/concurrentoolVO
npm install serialize-javascript@7.0.5  # Resolves serialize-javascript RCE + DoS
npm audit fix --force  # May upgrade @rollup/plugin-terser and workbox-build
```

**Verification:**
```bash
npm audit  # Should show only xlsx vulns remaining
```

### Unfixable (Direct Dependency)

**xlsx** has no patched versions available for both CVEs. Options:

1. **Switch to alternative:** Consider `exceljs` or `openxml` for safer Excel handling
2. **Isolate usage:** If xlsx is optional/non-critical, consider removing
3. **Accept risk:** Document in `code_context/security-posture.md` with mitigation details (input validation, DoS rate-limiting, etc.)

---

## Root Project

No dependency files detected (`package.json`, `pyproject.toml`, `Cargo.toml`).
